from setuptools import setup, find_packages

setup(
   name='fastapi_test',
   version='0.0.1',
   description='desciption',
   author='Simon Bonnefoy',
   author_email='',
   install_require=['fastapi'],
   packages=find_packages()
)
